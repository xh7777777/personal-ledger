services:
  personal-ledger:
    build: .
    ports:
      - "4173:4173"
    environment:
      - PORT=4173
      - HOST=0.0.0.0
      - DATA_DIR=/app/data
    volumes:
      - ledger-data:/app/data

volumes:
  ledger-data:
